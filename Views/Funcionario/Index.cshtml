﻿
@model IEnumerable<FuncionarioModel>


@{
    ViewData["Title"] = "Funcionários";
}
<div class="container">
<div class="text-center">
    <div class="d-grid gap-2 d-md-flex justify-content-md-start">
        <a class="btn btn-primary" role="button" asp-area="" asp-controller="Funcionario" asp-action="Cadastrar">Cadastrar</a>

    </div>

    @if(TempData["MensagemSucesso"] !=null)
    {
        <div class= "alert alert-success text-light" role="alert">
            @TempData["MensagemSucesso"]
            </div>

    }

      @if(TempData["MensagemErro"] !=null)
    {
        <div class= "alert alert-danger text-light" role="alert">
            @TempData["MensagemErro"]
            </div>

    }


    <h1 class="display-4">Funcionários</h1>
    <table id="Funcionario" class="table-funcionario">
        <thead>
            <tr>
                <th scope="col">#</th>
                <th scope="col">Nome</th>
                <th scope="col">Cargo</th>
                <th scope="col">Data de Admissão</th>
                <th scope="col"></th>
            </tr>
        </thead>
        <tbody>

            @if(Model != null)
            {
                @foreach( var funcionario in Model)
                {
                    <tr>
                        <th scope="row"></th>
                        <td>@funcionario.func_nome</td>
                        <td>@funcionario.func_cargo</td>
                        <td>@funcionario.func_dt_admissao</td>
                        
                        <td>
                            <div class="btn-group" role="group" aria-label="Basic example">
                                    <a role="button" class="btn btn-primary" asp-area="" asp-controller="Funcionario" asp-action="Editar">Editar</a>

                                <a role="button" class="btn btn-danger" asp-area="" asp-controller="Funcionario" asp-action="Excluir">Excluir</a>
                            </div>
                        </td>
                    </tr>
                   @* <tr>
                        <th scope="row">2</th>
                        <td>Jacob</td>
                        <td>Thornton</td>
                        <td>fat</td>
                        <td>
                            <div class="btn-group" role="group" aria-label="Basic example">
                                <a role="button" class="btn btn-primary" asp-controller="Funcionario" asp-action="Editar">Editar</a>

                                <a role="button" class="btn btn-danger" asp-controller="Funcionario" asp-action="Excluir">Excluir</a>
                            </div>
                        </td>
                    </tr>
                    <tr>
                        <th scope="row">3</th>
                        <td colspan="2">Larry the Bird</td>
                        <td>twitter</td>
                        <td>
                            <div class="btn-group" role="group" aria-label="Basic example">
                                <a role="button" class="btn btn-primary" asp-controller="Funcionario" asp-action="Editar">Editar</a>

                                <a role="button" class="btn btn-danger" asp-controller="Funcionario" asp-action="Excluir">Excluir</a>
                            </div>
                        </td>
                    </tr>*@
                }
            }
           
        </tbody>
    </table>
</div>
</div>